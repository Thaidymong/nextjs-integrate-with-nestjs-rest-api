// Defines the main application routes for consistent navigation and redirects.
export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  DASHBOARD: '/dashboard',
  PROFILE: '/profile',
  // Add other routes as needed
};
